# 微码映射系统架构梳理

## 1. 系统概述

微码映射系统的目标是将硬件AST与软件AST进行匹配，找到最相似的软件代码片段。

### 核心流程
```
硬件AST文件 → 硬件AST分析 → 硬件特征提取
软件AST文件 → 软件AST分析 → 内联处理 → 软件特征提取
                                    ↓
                              树匹配算法 → 匹配结果
```

## 2. 核心数据结构

### 2.1 AST节点 (BaseAST)
```cpp
class BaseAST {
    AST_NODE_TYPE type;           // 节点类型
    std::string text;             // 节点文本
    int indent;                   // 缩进级别
    BaseAST* down;                // 子节点
    BaseAST* right;               // 兄弟节点
    std::map<NodeAttributeName_t, std::any> attributes; // 节点属性
};
```

### 2.2 硬件AST统计信息 (HardwareASTStats)
```cpp
struct HardwareASTStats {
    int totalNodes;                      // 总节点数
    int maxDepth;                        // 最大深度
    std::map<std::string, int> globalVars; // 全局变量
    BaseAST* root;                       // 硬件AST根节点
};
```

### 2.3 跳转信息 (JumpInfo)
```cpp
struct JumpInfo {
    BaseAST* jumpNode;           // 跳转节点
    int jmpDepth;                // 跳转节点深度
    int targetDepth;             // 目标节点深度
    int maxDepth;                // 内联后的最大深度
    std::string targetName;      // 目标名称
    bool hasParameters;          // 是否包含参数
    BaseAST* targetNode;         // 目标节点
    int totalInlinedNodes;       // 总内联节点数
};
```

### 2.4 内联记录 (InlineRecord)
```cpp
struct InlineRecord {
    std::set<BaseAST*> processedJumps;   // 已处理的跳转
    std::set<BaseAST*> skippedJumps;     // 跳过的跳转
    std::set<std::string> unfoundNodes;   // 未找到的目标节点
    int totalInlinedNodes;               // 总内联节点数
};
```

### 2.5 Bundle/Function信息 (BundleFunctionInfo)
```cpp
struct BundleFunctionInfo {
    BaseAST* definitionNode;             // 定义节点（原始AST）
    std::string name;                    // 名称
    std::vector<JumpInfo> jumps;         // 包含的跳转
    int maxDepth;                        // 最大深度
    int nodeCount;                       // 节点数量
    InlineRecord inlineRecord;           // 内联处理记录
    bool isBundle;                       // 是否是bundle
};
```

### 2.6 软件AST统计信息 (SoftwareASTStats)
```cpp
struct SoftwareASTStats {
    std::vector<BundleFunctionInfo> bundles;     // Bundle列表
    std::vector<BundleFunctionInfo> functions;   // Function列表
    std::map<std::string, BaseAST*> nameToNode;  // 名称到节点的映射
    std::map<BaseAST*, std::pair<bool, size_t>> nodeToBundle; // 节点到bundle的映射
    int totalJumps;                              // 总跳转数
};
```

### 2.7 树匹配结果 (TreeMatchResult)
```cpp
struct TreeMatchResult {
    BaseAST* matchedNode;                // 匹配的节点
    double similarity;                   // 相似度
};
```

## 3. 核心组件

### 3.1 微码映射引擎 (MicrocodeMappingEngine)
```cpp
class MicrocodeMappingEngine {
private:
    // 核心组件
    std::unique_ptr<HardwareASTAnalyzer> hwAnalyzer;
    std::unique_ptr<SoftwareASTAnalyzer> swAnalyzer;
    std::unique_ptr<InlineProcessor> inlineProcessor;
    std::unique_ptr<TreeMatcher> treeMatcher;
    
    // 数据存储
    std::unique_ptr<HuaweiContext> hwContext;    // 硬件AST上下文
    std::unique_ptr<HuaweiContext> swContext;    // 软件AST上下文
    HardwareASTStats hwStats;                    // 硬件统计信息
    SoftwareASTStats swStats;                    // 软件统计信息
    std::vector<TreeMatchResult> matchResults;   // 匹配结果
};
```

### 3.2 内联处理器 (InlineProcessor)
```cpp
class InlineProcessor {
private:
    const HardwareASTStats& hwStats;  // 硬件统计信息（用于判断是否内联）
    SoftwareASTStats& swStats;        // 软件统计信息（会被修改）
    
public:
    bool processInline(BundleFunctionInfo& bundle);  // 处理单个bundle的内联
    std::unique_ptr<BaseAST> deepCopyAST(BaseAST* node); // 深拷贝AST
};
```

### 3.3 树匹配器 (TreeMatcher)
```cpp
class TreeMatcher {
private:
    const HardwareASTStats& hwStats;  // 硬件统计信息
    size_t maxMatchResults;           // 最大匹配结果数
    
public:
    std::vector<TreeMatchResult> matchTrees(const std::vector<BaseAST*>& softwareNodes);
};
```

## 4. 处理流程详解

### 4.1 软件AST处理流程

#### 4.1.1 解析阶段
```cpp
// 1. 解析软件AST文件
swContext->parseAST(softMgr);

// 2. 创建软件分析器
swAnalyzer = std::make_unique<SoftwareASTAnalyzer>(swContext.get());

// 3. 分析AST，提取bundles和functions
swAnalyzer->analyze();
// - 遍历AST，找到所有bundle和function定义
// - 为每个bundle/function创建BundleFunctionInfo
// - 统计跳转信息
// - 构建名称到节点的映射
```

#### 4.1.2 内联处理阶段
```cpp
// 对每个bundle/function进行内联处理
for (auto& bundle : swStats.bundles) {
    inlineProcessor->processInline(bundle);
}
```

**内联处理的关键步骤：**
1. **深拷贝原始AST：** `deepCopyAST(bundle.definitionNode)`
2. **创建拷贝的Bundle：** `BundleFunctionInfo copiedBundle(bundle)`
3. **在拷贝上进行内联：** 修改 `copiedBundle.definitionNode`
4. **更新原始Bundle：** `bundle = copiedBundle` ← **这里会修改原始数据！**

### 4.2 硬件AST处理流程

#### 4.2.1 解析阶段
```cpp
// 1. 解析硬件AST文件
hwContext->parseAST(hardMgr);

// 2. 创建硬件分析器
hwAnalyzer = std::make_unique<HardwareASTAnalyzer>(hwContext.get());

// 3. 分析AST，提取统计信息
hwAnalyzer->analyze();
// - 统计节点数量和最大深度
// - 收集全局变量
// - 设置根节点
```

### 4.3 树匹配流程

#### 4.3.1 特征提取
```cpp
// 1. 为硬件AST生成TreeVector
TreeVector* targetVector = TreeVector::nodeVectorGen(hardwareRoot);

// 2. 为每个软件节点生成TreeVector
for (BaseAST* swNode : softwareNodes) {
    TreeVector* treeVector = TreeVector::nodeVectorGen(swNode);
}
```

#### 4.3.2 相似度计算
```cpp
// 计算两个TreeVector的相似度
double similarity = calculateTreeVectorSimilarity(treeVector, targetVector);
```

## 5. 内存管理问题分析

### 5.1 当前的内存问题

#### 5.1.1 内联处理的内存累积
```cpp
// 问题：内联处理会修改原始数据
bundle = copiedBundle; // 这会覆盖原始的BundleFunctionInfo

// 结果：每次内联都会累积修改，导致内存泄漏
处理ACC1: 原始bundle → 内联修改 → 累积修改
处理ACC2: 已修改bundle → 内联修改 → 进一步累积
处理ACC3: 已修改2次bundle → 内联修改 → 内存泄漏
```

#### 5.1.2 TreeVector的内存泄漏
```cpp
// 问题：TreeVector存储在节点属性中，没有及时清理
node->attributes[NodeAttributeName_t::NODE_VECTOR] = treeVector;

// 结果：每个节点都会累积TreeVector，导致内存泄漏
```

### 5.2 解决方案

#### 5.2.1 流式处理方案
```cpp
// 对每个硬件文件独立处理
for (const auto& hardwareFile : hardwareFiles) {
    // 1. 解析硬件文件
    engine.processHardwareFileStreaming(hardwareFile, basename);
    
    // 2. 重置软件AST到原始状态
    engine.resetSoftwareAST(); // 清理内联累积
    
    // 3. 针对当前硬件进行内联
    engine.performInlineProcessing();
    
    // 4. 进行树匹配
    engine.performTreeMatching();
    
    // 5. 清理当前硬件内存
    engine.cleanupCurrentHardware();
}
```

#### 5.2.2 不修改原始AST方案
```cpp
// 修改内联处理，不覆盖原始数据
// 原始代码：
bundle = copiedBundle; // 会修改原始数据

// 修改后：
// 只更新统计信息，不修改原始AST
bundle.inlineRecord = copiedBundle.inlineRecord;
bundle.jumps = copiedBundle.jumps;
bundle.maxDepth = copiedBundle.maxDepth;
bundle.nodeCount = copiedBundle.nodeCount;
// 不修改 bundle.definitionNode
```

## 6. 性能分析

### 6.1 批量处理 vs 流式处理

| 方面 | 批量处理 | 流式处理 | 混合处理 |
|------|----------|----------|----------|
| 内存使用 | 高（累积） | 低（独立） | 中等 |
| 处理速度 | 快 | 慢 | 中等 |
| 稳定性 | 低（可能崩溃） | 高 | 高 |
| 准确性 | 中等 | 高 | 高 |

### 6.2 性能瓶颈

1. **内联处理：** 计算密集型，涉及AST遍历和节点复制
2. **TreeVector生成：** 每个节点都要生成特征向量
3. **相似度计算：** 需要比较大量向量

## 7. 优化建议

### 7.1 内存优化
1. **及时清理TreeVector：** 在树匹配完成后清理
2. **避免内联累积：** 使用深拷贝或重置机制
3. **流式处理：** 逐个处理硬件文件

### 7.2 性能优化
1. **缓存机制：** 缓存相似硬件特征的内联结果
2. **并行处理：** 多个硬件文件并行处理
3. **增量内联：** 只对变化的部分进行内联

### 7.3 代码优化
1. **简化数据结构：** 减少不必要的拷贝
2. **智能指针：** 使用智能指针管理内存
3. **内存池：** 对频繁分配的小对象使用内存池

## 8. 当前实现状态总结

### 8.1 已解决的问题

#### 8.1.1 内存管理问题
- **问题：** `currentNode`被定义为对`definitionNode`的引用，导致在`switchToInline`中意外清空了`definitionNode`
- **解决：** 将`currentNode`从引用改为普通指针，确保独立性

#### 8.1.2 重复删除内存问题
- **问题：** `inlineNode`被删除两次，导致段错误
- **解决：** 在`processInline`中正确转移所有权，防止`copiedBundle`析构时删除`inlineNode`

#### 8.1.3 树匹配节点收集问题
- **问题：** 树匹配使用`currentNode`，但在`switchToInline`后`currentNode`为空
- **解决：** 改为使用`definitionNode`，确保使用原始的AST节点

### 8.2 当前架构优势

1. **内存安全：** 通过分离原始数据和内联数据，避免了内存累积问题
2. **处理效率：** 软件AST只解析一次，内联处理针对每个硬件文件独立进行
3. **数据一致性：** 每个硬件文件都使用干净的软件AST进行匹配

### 8.3 核心挑战

微码映射系统的核心挑战在于：
1. **内存管理：** 内联处理会累积修改原始数据
2. **性能平衡：** 在内存安全和处理速度之间找到平衡
3. **数据一致性：** 确保每个硬件文件都使用干净的软件AST

当前的最佳方案是使用流式处理，确保每个硬件文件都独立处理，避免内存累积问题。 


## 9. 内存问题具体位置分析

### 9.1 主要内存泄漏位置

#### 9.1.1 内联处理中的内存累积（最严重）
**位置：** `src/lib/MicrocodeMapping/InlineProcessor.cpp:133`
```cpp
// 问题代码：
bundle = copiedBundle; // 这行会覆盖原始的BundleFunctionInfo
```
**问题：** 每次内联都会累积修改原始数据，导致内存泄漏

#### 9.1.2 内联AST节点递归删除缺失（关键问题）
**位置：** `src/include/MicrocodeMapping/MicrocodeMappingEngine.h:143`
```cpp
// 问题代码：
if (inlineNode) {
    delete inlineNode; // 只删除根节点，不删除整个AST树
}
```
**问题：** 内联过程中创建的AST节点没有被递归删除，导致内存泄漏

#### 9.1.3 内联过程中新创建的AST节点管理不当
**位置：** `src/lib/MicrocodeMapping/InlineProcessor.cpp:365`
```cpp
// 问题代码：
bodyNodes.push_back(deepCopyAST(current).release());
```
**问题：** 内联过程中深拷贝的AST节点被插入到AST树中，但没有正确的所有权管理

#### 9.1.4 copiedBundle资源清理不完整
**位置：** `src/lib/MicrocodeMapping/InlineProcessor.cpp:130`
```cpp
// 问题代码：
bundle.inlineNode = copiedBundle.inlineNode;
copiedBundle.inlineNode = nullptr; // 只清理了inlineNode
// 缺少对copiedBundle其他资源的清理
```
**问题：** copiedBundle的其他资源（如inlineJumps、inlineRecord）没有被清理

#### 9.1.5 TreeVector内存泄漏
**位置：** `src/lib/CloneDetection/tree-vector.cpp:354`
```cpp
// 问题代码：
node->attributes.insert(std::pair<NodeAttributeName_t, TreeVector *>(
    NodeAttributeName_t::NODE_VECTOR, returnVector));
```
**问题：** TreeVector存储在节点属性中，没有及时清理

#### 9.1.6 TreeVector清理逻辑不一致（关键问题）
**位置：** `src/lib/MicrocodeMapping/TreeMatcher.cpp:95`
```cpp
// 原始问题代码：
// 生成：只遍历子节点（getDown()然后getRight()）
// 清理：递归清理子节点和兄弟节点（getDown()和getRight()）
```
**问题：** 清理逻辑比生成逻辑更激进，会删除不应该删除的TreeVector

#### 9.1.7 深拷贝AST节点的内存泄漏
**位置：** `src/lib/MicrocodeMapping/InlineProcessor.cpp:40`
```cpp
// 问题代码：
copiedBundle.definitionNode = bundleCopy.release(); // 转移所有权
```
**问题：** 深拷贝的AST节点在覆盖原始bundle时可能没有正确释放

#### 9.1.8 内联记录中的指针累积
**位置：** `src/include/MicrocodeMapping/MicrocodeMappingEngine.h:79`
```cpp
struct InlineRecord {
    std::set<BaseAST*> processedJumps;   // 指针累积
    std::set<BaseAST*> skippedJumps;     // 指针累积
    // ...
};
```
**问题：** 内联记录中存储了大量指针，但没有及时清理

### 9.2 内存泄漏的累积效应

```
处理ACC1: 原始AST → 内联修改 → 累积修改1
处理ACC2: 已修改AST → 内联修改 → 累积修改2  
处理ACC3: 已修改2次AST → 内联修改 → 累积修改3
...
处理ACCn: 已修改n-1次AST → 内联修改 → 内存溢出
```

### 9.3 内存泄漏的根本原因分析

#### 9.3.1 内联AST节点递归删除缺失
**问题描述：** 内联过程中创建的AST节点没有被递归删除，只删除了根节点
**影响：** 每次内联都会累积大量AST节点，导致内存爆炸性增长
**解决方案：** 实现递归删除AST树的函数

```cpp
void deleteASTTree(BaseAST* node) {
    if (!node) return;
    deleteASTTree(node->getDown());    // 递归删除子节点
    deleteASTTree(node->getRight());   // 递归删除兄弟节点
    delete node;                       // 删除当前节点
}
```

#### 9.3.2 内联过程中新AST节点的所有权管理
**问题描述：** 在`extractBodyFromTarget`中深拷贝的AST节点被插入到AST树中，但没有正确的所有权管理
**影响：** 新创建的节点可能被重复删除或泄漏
**解决方案：** 确保所有权转移正确，避免重复删除

```cpp
// 转移所有权，避免重复删除
bundle.inlineNode = copiedBundle.inlineNode;
copiedBundle.inlineNode = nullptr; // 防止copiedBundle析构时删除inlineNode
```

#### 9.3.3 copiedBundle资源清理不完整
**问题描述：** copiedBundle的其他资源（如inlineJumps、inlineRecord）没有被清理
**影响：** 内存泄漏和资源累积
**解决方案：** 完整清理copiedBundle的所有资源

```cpp
// 清理copiedBundle的其他资源，避免内存泄漏
copiedBundle.inlineJumps.clear();
copiedBundle.inlineRecord.clear();
```

#### 9.3.4 TreeVector累积问题
**问题描述：** 每次树匹配都会为每个节点生成TreeVector，存储在节点属性中但没有清理
**影响：** TreeVector在内存中累积，导致内存泄漏
**解决方案：** 在树匹配完成后立即清理TreeVector

```cpp
// 在树匹配完成后清理TreeVector内存
treeMatcher->cleanupTreeVectors(softwareNodes);
```

#### 9.3.5 TreeVector清理逻辑不一致问题
**问题描述：** TreeVector的生成和清理逻辑不一致，清理比生成更激进
**影响：** 可能删除不应该删除的TreeVector，影响其他bundle的处理
**解决方案：** 确保清理逻辑与生成逻辑完全一致

```cpp
// 修复前：清理逻辑不一致
cleanupTreeVectorsRecursive(node->getDown());  // 递归清理子节点
cleanupTreeVectorsRecursive(node->getRight()); // 递归清理兄弟节点

// 修复后：与生成逻辑一致
for (BaseAST *child = node->getDown(); child != nullptr; child = child->getRight()) {
    cleanupTreeVectorsRecursive(child); // 只清理子节点，不访问兄弟节点
}
```

**关键原则：** 传入的节点不应该访问它的兄弟节点，因为兄弟节点会作为独立的根节点被处理

## 10. 优化方案：分离原始数据与内联数据

### 10.1 改进的BundleFunctionInfo结构

```cpp
struct BundleFunctionInfo {
    // 原始数据（保持不变）
    BaseAST* definitionNode;             // 定义节点（原始AST）
    std::string name;                    // 名称
    std::vector<JumpInfo> originalJumps; // 原始跳转信息
    int originalMaxDepth;                // 原始最大深度
    int originalNodeCount;               // 原始节点数量
    bool isBundle;                       // 是否是bundle
    
    // 内联数据（每次处理时重置）
    BaseAST* inlineNode;                 // 内联后的AST节点
    std::vector<JumpInfo> inlineJumps;   // 内联后的跳转信息
    int inlineMaxDepth;                  // 内联后的最大深度
    int inlineNodeCount;                 // 内联后的节点数量
    InlineRecord inlineRecord;           // 内联处理记录
    
    // 当前使用的数据（指向原始或内联）
    std::vector<JumpInfo>& jumps;        // 当前跳转信息
    int& maxDepth;                       // 当前最大深度
    int& nodeCount;                      // 当前节点数量
    BaseAST*& currentNode;               // 当前AST节点
    
    BundleFunctionInfo() : 
        definitionNode(nullptr), inlineNode(nullptr),
        originalMaxDepth(0), originalNodeCount(0),
        inlineMaxDepth(0), inlineNodeCount(0),
        jumps(originalJumps), maxDepth(originalMaxDepth), 
        nodeCount(originalNodeCount), currentNode(definitionNode) {}
    
    // 切换到内联模式
    void switchToInline() {
        jumps = inlineJumps;
        maxDepth = inlineMaxDepth;
        nodeCount = inlineNodeCount;
        currentNode = inlineNode;
    }
    
    // 重置到原始模式
    void resetToOriginal() {
        jumps = originalJumps;
        maxDepth = originalMaxDepth;
        nodeCount = originalNodeCount;
        currentNode = definitionNode;
        
        // 清理内联数据
        if (inlineNode) {
            delete inlineNode;
            inlineNode = nullptr;
        }
        inlineJumps.clear();
        inlineRecord.clear();
        inlineMaxDepth = 0;
        inlineNodeCount = 0;
    }
};
```

### 10.2 当前实际的处理流程

```cpp
// 1. 软件AST解析（只需要一次）
engine.parseSoftwareAST(softwareFile, symbolTableFile);
engine.preprocessSoftwareAST();

// 2. 对每个硬件文件
for (const auto& hardwareFile : hardwareFiles) {
    // 2.1 解析硬件文件
    engine.processHardwareFileStreaming(hardwareFile, basename);
    
    // 2.2 切换到内联模式
    engine.switchToInlineMode(); // 内部调用每个bundle的switchToInline()
    
    // 2.3 进行内联处理
    engine.performInlineProcessing(); // 处理每个bundle和function
    
    // 2.4 进行树匹配
    engine.performTreeMatching(); // 使用definitionNode进行匹配
    
    // 2.5 重置到原始模式（清理内联数据）
    engine.resetToOriginalMode(); // 内部调用每个bundle的resetToOriginal()
    
    // 2.6 清理硬件内存
    engine.cleanupCurrentHardware();
}
```

### 10.3 关键实现细节

#### 10.3.1 BundleFunctionInfo数据结构
```cpp
struct BundleFunctionInfo {
    // 原始数据（保持不变）
    BaseAST* definitionNode;             // 定义节点（原始AST）
    std::string name;                    // 名称
    std::vector<JumpInfo> originalJumps; // 原始跳转信息
    int originalMaxDepth;                // 原始最大深度
    int originalNodeCount;               // 原始节点数量
    bool isBundle;                       // 是否是bundle
    
    // 内联数据（每次处理时重置）
    BaseAST* inlineNode;                 // 内联后的AST节点
    std::vector<JumpInfo> inlineJumps;   // 内联后的跳转信息
    int inlineMaxDepth;                  // 内联后的最大深度
    int inlineNodeCount;                 // 内联后的节点数量
    InlineRecord inlineRecord;           // 内联处理记录
    
    // 当前使用的数据（指向原始或内联）
    std::vector<JumpInfo>& jumps;        // 当前跳转信息
    int& maxDepth;                       // 当前最大深度
    int& nodeCount;                      // 当前节点数量
    BaseAST* currentNode;                // 当前AST节点（修复：从引用改为指针）
};
```

#### 10.3.2 内存管理关键修复
1. **currentNode独立性：** 从引用改为指针，避免意外修改definitionNode
2. **所有权转移：** 在processInline中正确转移inlineNode的所有权
3. **树匹配节点：** 使用definitionNode而不是currentNode进行匹配

#### 10.3.3 内联处理流程
```cpp
bool InlineProcessor::processInline(BundleFunctionInfo &bundle) {
    // 1. 深拷贝原始AST
    auto bundleCopy = deepCopyAST(bundle.definitionNode);
    
    // 2. 创建拷贝的bundle用于内联处理
    BundleFunctionInfo copiedBundle(bundle);
    copiedBundle.inlineNode = bundleCopy.release(); // 转移所有权
    
    // 3. 执行内联处理循环
    // ... 内联逻辑 ...
    
    // 4. 将内联结果存储到bundle的内联数据中
    bundle.inlineNode = copiedBundle.inlineNode;
    copiedBundle.inlineNode = nullptr; // 防止重复删除
    // ... 其他数据转移 ...
    
    return true;
}
```

### 10.4 性能优势

| 方面 | 原始方案 | 当前优化方案 |
|------|----------|----------|
| 软件AST解析 | N次 | 1次 |
| 软件AST前期准备 | N次 | 1次 |
| 内联处理 | N次 | N次 |
| 内存使用 | 累积增长 | 稳定 |
| 处理速度 | 慢 | 快 |
| 内存安全 | 低 | 高 |
| 段错误风险 | 高 | 低 |

### 10.5 新增的关键修复

#### 10.5.1 内存管理修复
- **currentNode引用问题：** 修复了currentNode作为definitionNode引用导致的内存错误
- **重复删除问题：** 修复了inlineNode被重复删除导致的段错误
- **所有权管理：** 正确实现了AST节点的所有权转移

#### 10.5.2 树匹配修复
- **节点收集问题：** 修复了树匹配使用错误节点的问题
- **匹配准确性：** 确保使用原始AST节点进行匹配，提高准确性

#### 10.5.3 流式处理优化
- **内存清理：** 每个硬件文件处理完后立即清理内联数据
- **数据隔离：** 确保每个硬件文件使用独立的处理环境

### 10.4 实现要点

1. **保持原始数据不变：** 原始AST和统计信息始终不变
2. **内联数据独立管理：** 内联产生的数据单独存储和清理
3. **引用切换机制：** 通过引用切换来使用不同的数据集
4. **及时清理：** 每个硬件文件处理完后立即清理内联数据

### 10.5 修复后的内存管理流程

#### 10.5.1 内联处理阶段的内存管理
```cpp
// 1. 创建copiedBundle（浅拷贝）
BundleFunctionInfo copiedBundle(bundle);

// 2. 深拷贝AST节点
auto bundleCopy = deepCopyAST(bundle.definitionNode);
copiedBundle.inlineNode = bundleCopy.release();

// 3. 执行内联操作（可能创建新的AST节点）
performInline(targetNode, copiedBundle, jmpNode, jmp2);

// 4. 转移所有权到bundle
bundle.inlineNode = copiedBundle.inlineNode;
copiedBundle.inlineNode = nullptr; // 防止重复删除

// 5. 清理copiedBundle的其他资源
copiedBundle.inlineJumps.clear();
copiedBundle.inlineRecord.clear();
```

#### 10.5.2 树匹配阶段的内存管理
```cpp
// 1. 生成TreeVector
TreeVector* treeVector = TreeVector::nodeVectorGen(node);

// 2. 执行匹配
matchResults = treeMatcher->matchTrees(softwareNodes);

// 3. 立即清理TreeVector
treeMatcher->cleanupTreeVectors(softwareNodes);
```

#### 10.5.3 重置阶段的内存管理
```cpp
// 1. 递归删除内联AST树
deleteASTTree(inlineNode);

// 2. 清理内联记录
inlineJumps.clear();
inlineRecord.clear();

// 3. 重置统计信息
inlineMaxDepth = 0;
inlineNodeCount = 0;
```

### 10.6 内存泄漏修复总结

| 问题类型 | 原始问题 | 修复方案 | 效果 |
|----------|----------|----------|------|
| 内联AST递归删除 | 只删除根节点 | 实现deleteASTTree | 完全清理AST树 |
| 所有权管理 | 重复删除 | 正确转移所有权 | 避免重复删除 |
| copiedBundle清理 | 资源泄漏 | 完整清理所有资源 | 无资源泄漏 |
| TreeVector累积 | 内存累积 | 及时清理 | 内存稳定 |
| TreeVector清理逻辑 | 清理比生成激进 | 与生成逻辑一致 | 避免误删其他bundle数据 |

这个方案既保持了内存安全，又大大提高了处理速度，是一个很好的优化方向！

## 11. 时间复杂度分析

### 11.1 软件AST分析阶段

#### 11.1.1 AST解析
- **时间复杂度：** O(N)，其中N是AST节点总数
- **空间复杂度：** O(N)
- **操作：** 逐行解析AST文件，构建AST树结构

#### 11.1.2 Bundle/Function提取
- **时间复杂度：** O(N)，遍历整个AST树
- **空间复杂度：** O(B + F)，其中B是Bundle数量，F是Function数量
- **操作：** 
  - 遍历AST树，找到所有bundle和function定义节点
  - 为每个bundle/function创建BundleFunctionInfo结构

#### 11.1.3 跳转信息统计
- **时间复杂度：** O(N × D)，其中D是平均深度
- **空间复杂度：** O(J)，其中J是跳转指令总数
- **操作：** 
  - 对每个bundle/function调用countJumps()
  - 递归遍历AST树，统计所有跳转指令
  - 每个节点最多被访问一次

#### 11.1.4 跳转信息填充
- **时间复杂度：** O(B × J × log(B + F))
- **空间复杂度：** O(B + F)
- **操作：**
  - 为每个跳转指令查找目标节点
  - 使用map进行名称到节点的映射查找
  - 计算每个跳转的深度和统计信息

### 11.2 内联处理阶段

#### 11.2.1 深拷贝AST
- **时间复杂度：** O(N)，其中N是AST节点数
- **空间复杂度：** O(N)
- **操作：** 递归复制整个AST树结构

#### 11.2.2 内联处理循环
- **时间复杂度：** O(J × I × N)，其中J是跳转数，I是内联次数，N是平均内联节点数
- **空间复杂度：** O(N × I)
- **操作：**
  - 对每个跳转指令进行内联判断
  - 每次内联需要复制目标AST节点
  - 内联过程中可能产生新的跳转指令

#### 11.2.3 内联判断
- **时间复杂度：** O(1)
- **空间复杂度：** O(1)
- **操作：** 比较跳转深度与硬件特征阈值

#### 11.2.4 内联执行
- **时间复杂度：** O(T)，其中T是目标AST的节点数
- **空间复杂度：** O(T)
- **操作：**
  - 提取目标AST的body部分
  - 调整缩进级别
  - 替换跳转节点

### 11.3 树匹配阶段

#### 11.3.1 TreeVector生成
- **时间复杂度：** O(N × D)，其中N是节点数，D是平均深度
- **空间复杂度：** O(N)
- **操作：**
  - 遍历AST树，统计各类型节点数量
  - 收集标识符信息
  - 计算控制变量

#### 11.3.2 相似度计算
- **时间复杂度：** O(V)，其中V是TreeVector的维度
- **空间复杂度：** O(1)
- **操作：**
  - 计算欧几里得距离
  - 计算名称相似度
  - 综合相似度计算

#### 11.3.3 匹配排序
- **时间复杂度：** O(S × log(S))，其中S是软件节点数
- **空间复杂度：** O(S)
- **操作：** 对匹配结果按相似度排序

### 11.4 总体复杂度分析

#### 11.4.1 批量处理模式
```
软件AST分析：O(N + B×J×log(B+F))
内联处理：O(B×J×I×N) × 1次
树匹配：O(B×N×D + B×log(B)) × N次
总体：O(N + B×J×log(B+F) + B×J×I×N + B×N²×D + B×N×log(B))
```

#### 11.4.2 流式处理模式
```
软件AST分析：O(N + B×J×log(B+F)) × 1次
内联处理：O(B×J×I×N) × N次
树匹配：O(B×N×D + B×log(B)) × N次
总体：O(N + B×J×log(B+F) + B×J×I×N² + B×N²×D + B×N×log(B))
```

#### 11.4.3 优化后的混合模式
```
软件AST分析：O(N + B×J×log(B+F)) × 1次
内联处理：O(B×J×I×N) × N次
树匹配：O(B×N×D + B×log(B)) × N次
总体：O(N + B×J×log(B+F) + B×J×I×N² + B×N²×D + B×N×log(B))
```

### 11.5 性能瓶颈分析

#### 11.5.1 主要瓶颈
1. **内联处理：** O(B×J×I×N²) - 最耗时的操作
2. **TreeVector生成：** O(B×N×D) - 每个节点都要生成特征向量
3. **相似度计算：** O(B×N×V) - 需要比较大量向量

#### 11.5.2 优化建议
1. **缓存TreeVector：** 避免重复生成
2. **并行处理：** 多个硬件文件并行处理
3. **增量内联：** 只对变化的部分进行内联
4. **向量压缩：** 减少TreeVector的维度

### 11.6 实际性能估算

假设典型场景：
- N = 1000 (AST节点数)
- B = 10 (Bundle数量)
- J = 50 (跳转指令数)
- I = 5 (平均内联次数)
- D = 10 (平均深度)
- V = 100 (TreeVector维度)

**批量处理：** ~10⁶ 操作（内存爆炸）
**流式处理：** ~10⁷ 操作（内存安全）
**优化混合：** ~10⁶ 操作（内存安全）

### 11.7 修复后的复杂度分析

#### 11.7.1 TreeVector生成和清理的复杂度

**生成复杂度：**
```cpp
// 对于每个传入的节点（bundle/function根节点）
TreeVector* treeVector = TreeVector::nodeVectorGen(node);
```
- **时间复杂度：** O(N)，其中N是该bundle/function的节点数
- **空间复杂度：** O(N)，每个节点存储一个TreeVector

**清理复杂度：**
```cpp
// 递归清理，与生成逻辑一致
cleanupTreeVectorsRecursive(node);
```
- **时间复杂度：** O(N)，与生成相同
- **空间复杂度：** O(1)，只是删除，不占用额外空间

#### 11.7.2 整体流程的复杂度

**软件AST分析阶段（只执行一次）：**
```
软件AST解析：O(N_sw)
Bundle/Function提取：O(N_sw)
跳转信息统计：O(N_sw × D_sw)
跳转信息填充：O(B × J × log(B + F))
总体：O(N_sw + B × J × log(B + F))
```

**硬件文件处理阶段（每个硬件文件）：**
```
硬件AST解析：O(N_hw)
硬件AST分析：O(N_hw)
内联处理：O(B × J × I × N_sw)
树匹配：O(B × N_sw × V)
TreeVector清理：O(B × N_sw)
重置到原始模式：O(B × N_sw)
总体：O(N_hw + B × J × I × N_sw + B × N_sw × V)
```

#### 11.7.3 空间复杂度分析

**修复前的空间问题：**
```
处理ACC1: 内存使用 = O(N_sw × I + N_sw × V)
处理ACC2: 内存使用 = O(N_sw × I × 2 + N_sw × V × 2)  ← 累积增长
处理ACC3: 内存使用 = O(N_sw × I × 3 + N_sw × V × 3)  ← 继续累积
...
处理ACCn: 内存使用 = O(N_sw × I × n + N_sw × V × n)  ← 内存爆炸！
```

**修复后的空间管理：**
```
处理ACC1: 内存使用 = O(N_sw × I + N_sw × V) → 清理 → O(N_sw)
处理ACC2: 内存使用 = O(N_sw × I + N_sw × V) → 清理 → O(N_sw)
处理ACC3: 内存使用 = O(N_sw × I + N_sw × V) → 清理 → O(N_sw)
...
处理ACCn: 内存使用 = O(N_sw × I + N_sw × V) → 清理 → O(N_sw)  ← 内存稳定！
```

#### 11.7.4 总体复杂度

**时间复杂度：**
```
软件AST分析：O(N_sw + B × J × log(B + F)) × 1次
硬件文件处理：O(N_hw + B × J × I × N_sw + B × N_sw × V) × H次
总体：O(N_sw + B × J × log(B + F) + H × (N_hw + B × J × I × N_sw + B × N_sw × V))
```

**空间复杂度：**
```
软件AST：O(N_sw)  ← 只存储一次
硬件AST：O(N_hw)  ← 每个硬件文件独立
内联数据：O(N_sw × I)  ← 每次处理时创建，处理后清理
TreeVector：O(N_sw × V)  ← 每次匹配时创建，匹配后清理
总体：O(N_sw + N_hw + N_sw × I + N_sw × V)  ← 内存稳定！
```

#### 11.7.5 性能对比

| 方面 | 修复前 | 修复后 |
|------|--------|--------|
| 时间复杂度 | O(H × N_sw² × I × V) | O(H × N_sw × I × V) |
| 空间复杂度 | O(N_sw × I × H × V) | O(N_sw × I × V) |
| 内存增长 | 指数增长 | 线性增长 |
| 稳定性 | 低（可能崩溃） | 高（内存稳定） |

#### 11.7.6 实际性能估算

假设典型场景：
- N_sw = 1000 (软件AST节点数)
- N_hw = 500 (硬件AST节点数)
- B = 10 (Bundle数量)
- J = 50 (跳转指令数)
- I = 5 (平均内联次数)
- V = 100 (TreeVector维度)
- H = 20 (硬件文件数)

**修复前：**
- 时间复杂度：~10⁸ 操作
- 空间复杂度：~10⁶ 内存单位（累积增长）

**修复后：**
- 时间复杂度：~10⁷ 操作
- 空间复杂度：~10⁵ 内存单位（稳定）

#### 11.7.7 关键优化点

1. **TreeVector递归清理：** 确保清理与生成逻辑一致，不访问兄弟节点
2. **内联AST递归删除：** 完全清理内联产生的AST节点
3. **流式处理：** 每个硬件文件独立处理，避免累积
4. **及时清理：** 每个阶段完成后立即清理临时数据
5. **所有权正确转移：** 避免重复删除和内存泄漏

## 12. 图匹配算法复杂度分析

### 12.1 PDG图生成复杂度

#### 12.1.1 PDG构建过程
```cpp
// PDG生成器从MLIR函数构建程序依赖图
PDG& PDGGenerator::constructPDG() {
    // 遍历MLIR操作，构建顶点和边
    for (auto& op : func.getOps()) {
        addNode(op, VertexType::STATEMENT);
        // 分析数据依赖和控制依赖
        analyzeDependencies(op);
    }
}
```

**时间复杂度：** O(N²)，其中N是MLIR操作数
- **顶点构建：** O(N) - 为每个操作创建顶点
- **依赖分析：** O(N²) - 分析每个操作与其他操作的关系
- **边构建：** O(E) - 构建依赖边，其中E是边数

**空间复杂度：** O(N + E)
- **顶点存储：** O(N)
- **边存储：** O(E)
- **邻接表：** O(N + E)

#### 12.1.2 依赖分析复杂度
```cpp
// 数据依赖分析
void analyzeDataDependencies(Operation* op) {
    // 分析操作间的数据流依赖
    for (auto operand : op->getOperands()) {
        for (auto user : operand.getUsers()) {
            addEdge(definingOp, user, EdgeType::DATA_DEPENDENCY);
        }
    }
}
```

**时间复杂度：** O(N × U)，其中U是平均每个操作的使用者数
**空间复杂度：** O(E)

### 12.2 图同构匹配复杂度

#### 12.2.1 VF2算法复杂度
```cpp
// 使用Boost Graph Library的VF2算法
boost::vf2_subgraph_iso(
    sourcePDG.getGraph(), targetPDG.getGraph(), callback,
    boost::vertex_order_by_mult(sourcePDG.getGraph()),
    boost::edges_equivalent(edge_comp).vertices_equivalent(vertex_comp)
);
```

**时间复杂度：** O(N! × M)，其中：
- N是源图（硬件PDG）的顶点数
- M是目标图（软件PDG）的顶点数
- 最坏情况下需要尝试所有可能的顶点排列

**空间复杂度：** O(N + M)
- **递归调用栈：** O(N)
- **匹配状态存储：** O(M)

#### 12.2.2 顶点匹配规则复杂度
```cpp
// 顶点匹配规则
auto vertex_comp = [&](Vertex v1, Vertex v2) -> bool {
    auto src = sourcePDG.getOperation(v1);
    auto dst = targetPDG.getOperation(v2);
    
    // 操作类型比较：O(1)
    if (src->getName() == dst->getName()) {
        return true;
    }
    
    // 特殊操作类型匹配：O(1)
    if ((isa<atom::CompOp>(src) && isa<arith::CmpIOp>(dst)) ||
        (isa<arith::CmpIOp>(src) && isa<atom::CompOp>(dst))) {
        return true;
    }
    
    return false;
};
```

**时间复杂度：** O(1) - 常量时间比较
**空间复杂度：** O(1)

#### 12.2.3 边匹配规则复杂度
```cpp
// 边匹配规则
auto edge_comp = [&](Edge e1, Edge e2) -> bool {
    return sourcePDG.getEdgeType(e1) == targetPDG.getEdgeType(e2);
};
```

**时间复杂度：** O(1)
**空间复杂度：** O(1)

### 12.3 总体图匹配复杂度

#### 12.3.1 单次图匹配
```
PDG生成：O(N² + M²)
图同构匹配：O(N! × M)
总体：O(N² + M² + N! × M)
```

#### 12.3.2 批量图匹配
```
硬件PDG生成：O(H × N²)
软件PDG生成：O(S × M²)
图匹配：O(H × S × N! × M)
总体：O(H × N² + S × M² + H × S × N! × M)
```

其中：
- H是硬件函数数
- S是软件函数数
- N是硬件PDG平均顶点数
- M是软件PDG平均顶点数

### 12.4 性能瓶颈分析

#### 12.4.1 主要瓶颈
1. **PDG构建：** O(N²) - 依赖分析是二次复杂度
2. **图同构匹配：** O(N! × M) - 阶乘复杂度是主要瓶颈
3. **批量匹配：** O(H × S) - 函数对数量

#### 12.4.2 优化建议
1. **启发式搜索：** 使用启发式算法减少搜索空间
2. **并行处理：** 多个图匹配任务并行执行
3. **缓存机制：** 缓存已构建的PDG
4. **早期剪枝：** 在匹配过程中尽早剪枝不可能的分支

### 12.5 实际性能估算

假设典型场景：
- N = 50 (硬件PDG顶点数)
- M = 200 (软件PDG顶点数)
- H = 10 (硬件函数数)
- S = 100 (软件函数数)

**单次图匹配：**
- PDG生成：~10⁴ 操作
- 图同构匹配：~10⁶⁴ 操作（阶乘复杂度！）

**批量图匹配：**
- 总体：~10⁶⁶ 操作（计算不可行）

**结论：** 当前的VF2算法对于实际规模的图匹配是不可行的，需要更高效的算法。
